Review the CodeSurf repo after today's wave (plans 007-011 merged, flag-gated canvas perf optimisations landed) and fix the highest-leverage remaining defects that are safely fixable in one pass. Known-good verification: `npm run typecheck` (tsgo; 5 pre-existing errors in packages/contex-relay only — judge per-file), targeted `node --test test/<file>.test.ts`. Do not touch: daemon auth model, electrobun runtime decision, App.tsx decomposition (deferred L-effort items).
