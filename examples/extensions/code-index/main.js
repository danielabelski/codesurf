'use strict'

// Placeholder backend entry for the archived desktop extension surface.
// The reusable indexing modules under lib/ are being ported to codesurf CLI.
module.exports = {
  activate(ctx) {
    ctx?.log?.('[code-index] desktop extension surface is inactive')
  },
}
